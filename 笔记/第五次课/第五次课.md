1. 映射 
- 对齐 筛选

2. depth 傅里叶 卷积 可以改depth 的 backbone
- 频域 类似depth 

3. 提取出来rgb的高频特征 再去和depth 比较
- 再把rgb的残差加进去

4. depth 用频域增强


5. Loss
-  协方差 监督信号
- 自监督也是 监督信号  rgb 和 depth 分母要分开 分两个池子

6. 训练方式可以调整，先学基本loss，先学对齐loss

7. 协方差 解码头

-------------
- 空间域 频域 原图提取出来 f f f高 f低 还原回空间 加回去
- loss f高 f低也可以作为一种loss 
- 对齐特征与空间 infonce 一种计算公式  特征欲空间更对齐
- 2种方式    有预测 多了个生成的东西 重建(rgb1,rgb1_1)       直接输入 对比(rgb1,rgb2,rgb3,depth1) 
- 也可以单独掩码做一个loss
- 每个loss 每个解码头，反向调整的时候自己看那个在就能被优化
- 
 
1. f高f低输入协方差里面算loss
2. 掩码 loss  (掩几个 rgb 猜 depth 可以多个 depth 猜rgb只能掩1个)
3. 协方差loss 2个小类  重建(掩码的生成图输入)   对比(直接rgb 和 depth)

1. 频域的图提取出来做一些操作比如平滑 增强一下 然后还原回空间域 然后加回去encoder 相当于改了encoder
